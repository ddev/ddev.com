# Token to authenticate GitHub REST and GraphQL API calls (and avoid narrow rate limiting)
GITHUB_TOKEN=

# Amplitude Dashboard REST API credentials for the "DDEV - Production" project (appId 468525)
# Used to fetch live usage-stats chart data at build time (see src/lib/amplitude.ts)
AMPLITUDE_API_KEY=
AMPLITUDE_SECRET_KEY=