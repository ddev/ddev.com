---
/**
 * Somewhat flexible component used to display the primary heading (`h1`) on most pages.
 */
export interface Props {
  // Required primary text
  title: string
  // Smaller text to be displayed beneath the primary heading
  subtitle?: string
  // A pre-formatted date string (used for blog posts)
  date?: string
  // Human-formatted name for the page author (used for blog posts)
  author?: string
  // URL for the author’s index page (used for blog posts)
  authorUrl?: string
  // URL to the author’s avatar image (used for blog posts)
  authorAvatar?: string
  // Human-formatted read time (used for blog posts)
  readTime?: string
}

const { title, subtitle, date, author, authorUrl, authorAvatar, readTime } =
  Astro.props
---

<div class="my-24">
  <h1 class="font-bold text-4xl">{title}</h1>
  {subtitle && <p class="text-2xl text-gray-600 my-4">{subtitle}</p>}

  <div class="flex items-center content-center my-6 font-mono">
    {
      author && (
        <div class="flex items-center content-center mr-2">
          {authorAvatar && (
            <div class="rounded-full overflow-hidden mr-2">
              <img src={authorAvatar} alt={author} class="block w-12 h-12" />
            </div>
          )}
          {author && (
            <div class="ml-1">
              <a href={authorUrl}>{author}</a>
            </div>
          )}
        </div>
      )
    }
    <p class="">
      {
        date && (
          <span>
            {" "}
            <span aria-hidden="true"> ·</span>
            {date}
          </span>
        )
      }{
        readTime && (
          <span>
            {" "}
            <span aria-hidden="true"> ·</span> {readTime}
          </span>
        )
      }
    </p>
  </div>
</div>
